
<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url( <%= image_path "banner.jpg" %>);">
  <div class="banner-content">
    <h1>HELMETT</h1>
    <p>Rent a real bike easily</p>
    <% if current_user %>
      <%= link_to "Start now", vehicles_path(anchor: 'vehicles'), class: "btn btn-primary btn-lg" %>
    <% else %>
       <%= link_to "Start now", new_user_session_path, class: "btn btn-primary btn-lg" %>

<!--         <a class="btn btn-primary btn-lg">Start now</a> -->
    <% end %>
  </div>
</div>
